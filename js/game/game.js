/*  game/game
    Exports the Phaser.Game instance as a module. */
define(new Phaser.Game(500, 600, Phaser.CANVAS, 'body', null, false, false));
// Use an 800x600 HTML5 canvas in the main HTML body, no initial state, not transparent, no antialiasing.

